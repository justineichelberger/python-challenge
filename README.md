# python-challenge
    For PyBank challenge:
        Text file summary is written to the /PyBank/analysis directory.
        Within the /PyBank/Resources folder I have created a new csv file called "updated_budget_data.csv" that contains both columns from the "budget_data.csv"             with an appended "Profit Fluctuation" column.
    For PyPoll challenge:
        Text file summary is written to /PyPoll/analysis directory.
